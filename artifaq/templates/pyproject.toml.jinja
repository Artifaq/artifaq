[tool.poetry]
name = "{{ project_name }}"
version = "0.1.0"
description = "A FastAPI project using my-fastapi-framework"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.12"
my-fastapi-framework = "^0.1.0"

[tool.poetry.dev-dependencies]
pytest = "^7.4.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"